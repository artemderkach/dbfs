kind: pipeline
name: default

workspace:
  base: /go
  src: src/github.com/mind-rot/dbfs

steps:
- name: deploy
  image: appleboy/drone-ssh
  host:
  - google.com
  username: root
  secrets: [ ssh_key ]
  script:
  - echo "test ssh"

- name: build
  image: golang:1.12.5-alpine3.9
  commands:
  - pwd
  - ls
  - go build .

